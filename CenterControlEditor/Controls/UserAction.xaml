<UserControl x:Class="CenterControlEditor.Controls.UserAction"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300" Loaded="InitUI"  Height="40"  MouseDown="SelectThis" MouseDoubleClick="EditThis"    >

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0">
            <Viewbox   x:Name="ActionContainer">
                <Label  HorizontalAlignment="Left"  HorizontalContentAlignment="Left" VerticalAlignment="Center" VerticalContentAlignment="Center"   x:Name="ActionName"   />
            </Viewbox>
        </Grid>
        <Grid Grid.Column="1">
            <Viewbox x:Name="ReceiveCommand" Stretch="Uniform">
                <Label  HorizontalAlignment="Right" HorizontalContentAlignment="Right" VerticalAlignment="Center" VerticalContentAlignment="Center" x:Name="CodeName"/>
            </Viewbox>
        </Grid>
    </Grid>
</UserControl>
